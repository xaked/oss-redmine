
  <% if User.current.allowed_to?(:view_sprints, @project) && @issue.agile_sprint %>
    <%= issue_fields_rows do |rows|
      rows.right l(:label_agile_sprint), @issue.agile_sprint
    end %>
  <% end %>
